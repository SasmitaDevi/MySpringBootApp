apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springboot-hello.fullname" . }}-test-connection"
  labels:
    {{- include "springboot-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springboot-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
