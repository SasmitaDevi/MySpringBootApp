apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-hello.fullname" . }}
  labels:
    {{- include "springboot-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "springboot-hello.selectorLabels" . | nindent 4 }}
